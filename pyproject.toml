[project]
name = "jonsim.com-dev-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit==4.5.1",
    "cook-render",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "tools/cook-render"
]

[tool.uv.sources]
cook-render = { workspace = true }
